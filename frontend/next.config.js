/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['content.production.cdn.art19.com','podcastaddict.com'],
  },
}

module.exports = nextConfig
